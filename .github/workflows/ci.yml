name: CI-CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:

  # 🔹 JOB 1 - BUILD
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Criar pasta build
        run: |
          mkdir build
          cp -r *.html build/
          cp -r CSS build/
          cp -r JS build/

      # ⚠️ ERRO PROPOSITAL AQUI
      - name: Upload artefato
        uses: actions/upload-artifact@v4
        with:
          name: site
          path: dist   # ← NÃO EXISTE (vai falhar)

  # 🔹 JOB 2 - DEPLOY
  deploy:
    runs-on: ubuntu-latest
    needs: build

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Download artefato
        uses: actions/download-artifact@v4
        with:
          name: site
          path: dist

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload para Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4